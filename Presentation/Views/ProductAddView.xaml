<UserControl x:Class="Presentation.Views.ProductAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Views"
             
             xmlns:viewmodels="clr-namespace:Presentation.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ProductAddViewModel}"
             
             mc:Ignorable="d" 
             Background="LightGray"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Foreground="{Binding StatusColor}" TextWrapping="Wrap" />

        <TextBlock Grid.Row="1" VerticalAlignment="Center" Text="{Binding Title, FallbackValue=Ny Produkt}" FontWeight="Bold" FontSize="18" Margin="20 20 20 20" />
       
        <StackPanel Grid.Row="2">
            <StackPanel Margin="20 0 0 10">
                <Label Content="Namn" />
                <TextBox Text="{Binding ProductData.Name}" Margin="0 0 20 0"/>
            </StackPanel>

            <StackPanel Margin="20 0 0 10">
                <Label Content="Pris (SEK)" />
                <TextBox Text="{Binding ProductData.Price}" Margin="0 0 20 0" />
            </StackPanel>

            <StackPanel Margin="20 0 0 10">
                <StackPanel Orientation="Horizontal" >
                    <Label Content="Kategori" />
                    <TextBlock Text="(valfri)" Margin="0 5 0 0" Style="{StaticResource MissingValueTextStyle}" />
                </StackPanel>
                <TextBox Text="{Binding ProductData.Category.Name}" Margin="0 0 20 0" />
            </StackPanel>

            <StackPanel Margin="20 0 0 10">
                <StackPanel Orientation='Horizontal'>
                    <Label Content="Tillverkare" />
                    <TextBlock Text="(valfri)" Margin="0 5 0 0" Style="{StaticResource MissingValueTextStyle}" />
                </StackPanel>
                <TextBox Text="{Binding ProductData.Manufacturer.Name}" Margin="0 0 20 0"  />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 20 0">
                <Button Command="{Binding SaveCommand}" Content="Avbryt" Width="70" Height="25" Margin="0,0,10,0" Padding="5 5" Background="LightBlue" FontSize="10" FontWeight="Bold"/>
                <Button Command="{Binding CancelCommand}" Content="Spara" Width="70" Height="25" Padding="5 5" Background="LightBlue" FontSize="10" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>
  

    </Grid>
</UserControl>
